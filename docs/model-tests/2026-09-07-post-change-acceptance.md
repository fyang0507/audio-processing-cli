# Full post-change acceptance

The full functional matrix on candidate `be070f5d9bfd1b5e6845fe3e5b04ebac5a255b45`, containing PRs #66–#68, found **no new product regression**. Five main cases passed; the strict/default Qwen timing case retained its earlier required-delivery failure. The removed-option and reusable-JSON feature checks passed. The full matrix's required delivery verdict is therefore **FAIL**, independently of those passing feature checks. The [compact result](../../model_tests/benchmark/results/2026-09-07-post-change-acceptance.json) records the candidate, configurations, primary artifact hashes, scoped verdicts and evidence limits. Independent evidence review passed with no unresolved material finding; its primary comparisons and reporting adjudications are retained as `independent-review.md` under the raw evidence root.

Raw private media, canonical results, readable exports, stage diagnostics and command records remain untracked under `tests/artifacts/acceptance/2026-09-07-post-change-be070f5/`. The scope was declared before execution. Every operator started without development history, in a separate external working directory, with only the absolute candidate executable, the unchanged shipped skill snapshot and public CLI guidance. All model/media work ran sequentially; no recovery, larger alignment tolerance, alternate stack or reduced request was used.

## Required functional delivery

| Case | Verdict | Primary evidence under the raw root |
| --- | --- | --- |
| A1 FireRed, full multispeaker original | PASS | `a-firered-restart/canonical.json`: complete, 62 segments, 246 words, both requested timing outcomes produced, no abstentions; native TXT and timed Markdown delivered. |
| A2 VibeVoice, same original | PASS | `a-vibevoice/canonical.json`: complete, 20 timed segments, 17 native turns, diarization and segment timing produced, no abstentions; native TXT and timed Markdown delivered. Three native events remain unattributed. |
| B demo enhancement | PASS | `b-demo/render.report.json` and `delivered.inspect.json`: one product-demo/RNNoise dry run and one MP4 render, applied treatment, encoded -16.02 LUFS / -2.48 dBTP, peak pass, decoded-duration delta 0 ms; native report summary/comparison delivered. |
| C mixed-language transcription and enhancement | PASS | `c-mixed/canonical.json`: complete, 8 segments, 60 words, both timings produced, no abstentions; TXT/timed Markdown followed by one product-demo/RNNoise dry run and M4A render. `enhanced.m4a.report.json` and delivered inspection record -16.28 LUFS / -2.41 dBTP, peak pass, duration delta +1.333 ms; summary/comparison delivered. |
| D1 Qwen floors only | PASS | `d1-qwen/canonical.json`: one full-source recognition, 15 segments containing only `segment_id` and `text`, complete, empty requested capabilities/outcomes and abstentions. Run stdout equals saved JSON bytes; native TXT, Markdown and later provenance Markdown preserve its text and hash. |
| D2 Qwen diarized, overlapping and timed | FAIL | `d2-qwen/canonical.json`: diarization and overlap produced, word timing abstained. Required timing fails; TXT, timed Markdown, SRT and VTT remain BLOCKED under the stopping rule. |
| Feature-specific negatives | PASS | `feature-refusals/`: inspect/enhance `--vad-model` and run TXT/Markdown (including receipt mode) each exit 2 before creating destinations; current help matches the new surface. D1's untimed SRT and timestamped Markdown also refuse at exit 2 without output. |

All canonical hashes remain unchanged after their permitted exports or post-stop inspection. `identity.before.json` and `identity.after.json` verify the editable install binding, clean frozen candidate, all 166 source/skill files, all eight copied skill/support files and the byte sizes/hashes of all three original recordings. The implementation source is unchanged since the combined suite passed 1,769 tests with 17 opt-in RNNoise live-filter skips; the pre-acceptance lint/format gate also passed. This live run separately exercised RNNoise on both enhancement fixtures. The final documentation/architecture checks passed all 125 cases, and the post-report lint/format gate passed.

## Retained Qwen timing failure

The current raw aligner request at `d2-qwen/logs/audio-transcribe-uylaizjm/004-aligner-t__1fo_x/request.json` supplies `turn_25` bounds `[96.570437, 97.996625]` seconds. Its `result.json` returns one word at `[96.57, 98.01]`. Canonical abstention `ab_4` records a 0.437 ms start overrun and 13.375 ms end overrun against the default 0.501 ms limit, with code `out_of_unit_bounds` and affected `seg_27`. The segment retains text and anonymous speaker S1, without words or segment bounds. The operator stopped after canonical inspection and made no export or recovery call.

The artifact contains 33 speaker-labeled segments, 31 turns, 237 supplied words, 16 zero-duration words, one overlap interval and six abstentions. Five abstentions identify scopes withheld from recognition; the sixth is the alignment rejection above. Useful recognition, speaker and overlap output does not satisfy the missing required timing, and `complete: true` does not imply exhaustive speech coverage. The raw diagnostic bundle makes the reason traceable; the diagnostic/remediation work associated with [#51](https://github.com/fyang0507/audio-processing-cli/issues/51) did not make the strict/default policy accept this boundary estimate.

Direct comparison found exact equality of this run's canonical segments, turns and abstentions with `tests/artifacts/acceptance/2026-09-06-boundary-recovery/d-qwen/d2.strict.canonical.json`. This is the retained strict-policy limitation, not a new regression in the JSON or VAD-option changes. No new issue was opened for this unchanged, already documented behavior.

## Baselines and measurement limits

The FireRed multispeaker and mixed-language segments/words/abstentions and VibeVoice segments/turns/abstentions exactly match the primary artifacts in `model_tests/benchmark_runs/e2e-remaining-2026-09-06/acceptance/`. Both delivered media files match their corresponding baseline bytes: demo MP4 SHA-256 `58b1622788f2a31de1a58c1f90125598f13bee446b2601e3e1d0321fa488d0a3`, mixed M4A `5d1af49f5127457d05a1e6611fec31a36c59e58b45869daa182a8b51ab1e0185`. D1's segments and abstentions also match the earlier issue #65 canonical. Run-specific metadata is not claimed equal. These comparisons preserve the exact scope of the prior evidence; they do not create transcript ground truth or a new listening score.

The demo retains its optional intermediate voice-level correction bound (-37.382 versus -30 dBFS) and encoded LRA miss (16.9 versus the 11 LU preference). Its nine fixed original regional checks pass, while fresh detection finds five different regional preference misses. The mixed report has empty top-level `unresolved`, but nested noise reduction still abstains for `no_clean_reference` and speech preservation for `not_measured`; those limits also apply to the demo. Fixed original regions and fresh detected regions are different measurement scopes. Both timeline checks cover decoded duration only, explicitly abstaining on content alignment and A/V sync. Recognition, speaker correctness, filler recall, acoustic boundaries and perceptual improvement were not required judgments in this mechanical regression scope and remain unmeasured.

## Operator and reporting evidence

The initial `a-firered/` operator failed during its first help call by assigning zsh's read-only `status` variable. The shell error and help streams remain saved; the native help exit was lost. That attempt's operator execution is FAIL and delivery BLOCKED, with no media/model run. An independently declared fresh operator restarted the unchanged case in `a-firered-restart/`; the failed setup record is preserved. This is not a FireRed product failure or a model fallback.

The completed operators used individual literal CLI commands, direct stream/exit capture and directly authored reports: 82 calls, with 76 recorded exits 0 and six predeclared exits 2. B and D2 retain printed exit observations in their command ledgers rather than standalone exit files. Their records retain exact argv/cwd and available clock observations, native timestamps or disclosed file-mtime observations; no complete precisely timestamped process journal is claimed. No custom recorder, wrapper or reporting program was created for this pass. The earlier issue #65 setup-recorder deviation remains preserved in its own historical record and is not reused as evidence of this pass's protocol.

The raw D2 report describes the unattempted export bundle as BLOCKED. The development adjudication makes the full required delivery **FAIL** because the required timing row failed. Its unmeasured quality rows remain NOT REQUIRED under the declared scope rather than becoming blocked requirements. D1 similarly labels accuracy assessment BLOCKED, and B labels independent video-stream-copy verification BLOCKED; both are optional unmeasured observations, NOT REQUIRED for their passing delivery checks. The complete MP4's equality to the accepted baseline is separate development evidence, not a video-copy measurement supplied by the CLI. All raw reports are preserved alongside `developer-review-notes.md`; these adjudications change no model output or historical operator statement.
