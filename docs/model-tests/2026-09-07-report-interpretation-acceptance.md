# Custom-clip report interpretation acceptance

The requested acceptance-reporting and measurement-scope changes pass their declared checks. Candidate `a667b727606f8899e506a7346dde864625446e69` was tested from the sole repository checkout, with its editable executable binding and shipped skill snapshot recorded before the fresh operators started. The [compact result](../../model_tests/benchmark/results/2026-09-07-report-interpretation-acceptance.json) names the original fixtures and exact scope.

The first change concerns developer documentation: required delivery checks, optional observations and operator/reporting errors have separate outcomes, and the report is written directly as Markdown or JSON. Independent review caught a template ambiguity about required human judgment; commit `ceb3b2e` includes every required judgment in the delivery table, with unavailable judgment producing BLOCKED. The documentation checks passed without another model run.

The second change concerns offline report comparison and its shipped guidance. Two fresh operators used only the candidate, its shipped skill/public help and the named saved reports from the full-original demo video and mixed-language clip. Each compared original inspection with the render report, and the render report with saved delivered inspection. The mixed-language operator ran three independent offline projections concurrently, then inspected their outputs. The scope was declared before execution: native report output, interval/reference interpretation, predictions versus delivered measurements, and preserved inputs. Rendering, transcription, fresh detection and a new listening judgment were not required for this reporting-only change.

| Case | Required report delivery | Original inspection → render | Render → delivered inspection | Recorded regional findings |
| --- | --- | --- | --- | --- |
| Demo video | PASS; 5 CLI calls, all exit 0 | Speech and non-speech intervals `same` | Both `different` | Nine fixed intervals inside target; five fresh candidates outside their inspection target, among seven total inspection rules |
| Mixed-language audio | PASS; 7 CLI calls, all exit 0 | Speech and non-speech intervals `same` | Speech `different`; non-speech `same` | Both non-speech collections empty; no regional success rate or missing final evaluation invented |

The operators distinguished the demo's fixed speech reference (17.792 seconds, delivered RMS −18.674 dBFS) from fresh detection (19.520 seconds, −19.468 dBFS). On the mixed-language clip, fixed speech spans total 20.544 seconds at delivered −15.627 dBFS, while fresh detection totals 19.568 seconds at −15.453 dBFS. Equal interval findings concern recorded coordinates only; they do not establish equal sample masks, levels, detection policies or quality.

Delivered program measurements remain the saved values: demo −16.02 LUFS, −2.48 dBTP, 16.9 LU LRA; mixed-language −16.28 LUFS, −2.41 dBTP, 4.0 LU LRA. The demo's five fresh relative-level misses and LRA above 11 LU remain optional observations for this task. Nested unmeasured denoising, speech-preservation, alignment and sync checks stay explicit. Neither these observations nor successful report delivery create a new listening verdict. Finer per-event dynamics and tighter LRA control remain outside the requested work.

Both operators wrote their reports directly and completed without command or reporting failure. Independent review checked native pointers/values, interval relations and rule denominators against the actual input JSON, then reviewed both written interpretations. It found no material defect. Suggestions about compact-output size and additional phase labels are nonblocking usability ideas; this run creates no further implementation backlog.

Implementation verification: `uv run --extra dev pytest` passed **1,741 tests**, with **17 optional RNNoise live-filter tests skipped** because `AUDIO_TEST_RNNOISE=1` was not selected. The focused report/boundary suite passed 94 tests. Independent review additionally checked 4,489 interval-pair combinations and 35 malformed-report cases. Ruff lint/format passed. No media-processing, model-stage, measurement-generation or export source changed.

The [local raw evidence](../../tests/artifacts/acceptance/2026-09-07-report-interpretation/) retains the predeclared scope, manifest, source/skill hashes, original command events and separate streams. [Demo findings](../../tests/artifacts/acceptance/2026-09-07-report-interpretation/b-demo/report-reading.md) and [mixed-language findings](../../tests/artifacts/acceptance/2026-09-07-report-interpretation/c-mixed/08-report.md) link their native outputs. All 185 frozen identity checks passed afterward, including the two private source clips and all eight saved input reports. Native tool records are retained; no separate complete timestamped shell journal is claimed. The two empty external operator directories were removed after their processes exited; no additional worktrees were created.

Historical processing and acceptance records remain unchanged. This is acceptance of the two requested reporting changes on custom-clip evidence, not a new evaluation framework or an assertion that every historical optional target passed.
