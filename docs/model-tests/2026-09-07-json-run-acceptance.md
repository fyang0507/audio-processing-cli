# JSON run and offline export acceptance

The scoped issue #65 feature and delivery verdicts are **PASS** on candidate `eac6dfb16c8b4b5f25a4f1caf00cacb4c6c345ab`. A fresh operator used only the candidate executable, a retained shipped-skill snapshot and public CLI guidance to recognize the full original `test-sample-multispeaker.m4a` once with Qwen 1.7B floors only, save normalized JSON, and produce native TXT, Markdown and provenance Markdown from that same JSON. The [compact result](../../model_tests/benchmark/results/2026-09-07-json-run-acceptance.json) records configuration, hashes, checks, scope and the separate protocol verdict.

Raw evidence remains untracked under `tests/artifacts/acceptance/2026-09-07-issue65/`: `scope.md` declares the narrower task before execution; `identity.before.json` and `identity.after.json` bind the editable executable and all 166 tracked source/skill files to the unchanged candidate; `skill/` retains all eight shipped skill/support files; and `d1-json/commands/` retains arguments, working directories, streams and exits. Private canonical/transcript files and six backend diagnostic files remain there rather than in the tracked result.

The recorded ASR `request.json` and `result.json` under `d1-json/diagnostics/audio-transcribe-wnwq43pu/002-asr-40uvz_ab/` record one processed unit spanning `[0, 139.284]` seconds on the original decoded timeline. The artifact `d1-json/canonical.json` contains 15 segments with only `segment_id` and `text`, processing `complete: true`, requested plan capabilities and outcomes `{}`, and abstentions `[]`. Timing, speakers, explicit range and coverage fields remain absent. Original-source identity and command scope establish the requested full processing interval; they do not establish recognition accuracy or exhaustive spoken-word coverage. The original hash remains `d5579a83d046f12ede2b98e21cc510c98e4015ad16310ef95c389fc2bd6a249c`; the saved canonical hash remains `1014c26dcfdfa9c220cd21998f580ffac515e2afcc24d42eae30585fa25485a8` after every export.

Independent review compared actual run stdout, canonical JSON, raw ASR request/result, exported bytes and refusal streams. Run stdout matches saved JSON byte for byte; all three exports preserve canonical segment text in order. Two removed run formats, SRT from untimed JSON and timestamped Markdown from untimed JSON all refuse at exit 2 without creating their destinations. The removed formats also create no diagnostic directory. These predeclared negatives pass their safety checks; timing and speaker delivery were not requirements of the floors-only task.

The combined implementation suite passed 1,769 tests with 17 optional RNNoise live-filter tests skipped, and the repository lint/format gate passed. Controlled tests additionally cover stdout-only runs without automatic result files, explicit JSON spelling, partial receipts, range/resume and overwrite/source protection, and the frozen Python human-output compatibility surface. Other live matrix rows were excluded before execution because model-stage, normalization, timing, diarization and export algorithms are unchanged; issue #64 separately exercises the enhancement model-location change on original media. No fresh model-quality or listening judgment is claimed.

## Operator protocol and evidence limits

The operator's raw report says operator/reporting PASS; the developer adjudication records **setup protocol FAIL** separately. Setup commands 01–09 used a custom subprocess logging helper, which the development procedure prohibits. The operator brief allowed non-audio logging tools without spelling out that restriction until the scheduling follow-up; this orchestration omission does not erase the deviation. Independent inspection confirmed that the helper only captured arguments, bytes and exits, with no retry, result transformation or provider bypass. Commands 10–17 used individual shell redirects and status records. Their independently verified recognition, exports and refusals remain valid, so the scoped product delivery remains PASS.

The raw operator report is preserved unchanged. Calls 10–17 have exact arguments/cwd, raw streams and exits, but no persisted start/end timestamps; a complete timestamped shell journal is not claimed. Fresh-context provenance comes from the no-history agent dispatch and external working directory, not from a canonical JSON field. Developer reporting passes by preserving those limits and keeping protocol, functional delivery and unrequested quality judgments separate.
